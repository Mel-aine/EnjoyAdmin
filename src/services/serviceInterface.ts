export interface ProductOption {
  "name"?: string,
  "api": string,
  "quantity": number,
  "inputField"?:String
  "Id"?: number;
}

export interface FormattedDates {
  startDate: string;
  endDate: string;
}
